Zombie
라이트맵 - 유니티는 라이팅 데이터 에셋을 사용하여 라이팅 효과의 실시간 연산량을 줄이며, 씬에 변화가 감지될 때마다
매번 새로운 라이팅 데이터 에셋을 생성

글로벌 일루미네이션
- 글로벌 일루미네이션은 물체의 표면에 직접 들어오는 빛뿐만 아니라 다른 물체의 표면에서 반사되어 들어온
간접광까지 표현함. 줄여서 GI라고 부름

애니메이션 - 블렌드 트리 
- 한 객체에 여러가지 애니메이션이 있을때 그 애니메이션의 전환을 부드럽게 할수있음

애니메이터 컨트롤러에 레이어를 두 개 이상 만들면 각 레이어에서 재생하는 애니메이션은 위에서 아래 순서로
덮어쓰기 방식으로 적용됨.

애니메이터의 레이어별로 부위를 다르게 적용하려면 아바타 마스크를 설정해야함

FixedUpdate는 물리 갱신 주기에 맞춰 실행됨

시네머신 - 카메라의 움직임을 손쉽게 제어하는 유니티 공식 패키지
- 시네머신 가상 카메라 컴포넌트의 Look At 필드에 플레이어 캐릭터를 할 당하는 순간 게임 창에 붉은색과 푸른색으로 그려진 영역과 안내선이 보임. 
- 이들은 카메라의 자연스러운 추적을 구현하는 데 사용하는 소프트존과 하드 리밋임. 
- 현재 게임 창에는 작아서 보이지 않지만 데드존이라는 영역도 있음
- 시네머신의 브레인 카메라는 실제 화면을 촬영하는 카메라임. 
- 시네머신의 가상 카메라는 브레인 카메라의 분신 역할을 함. 
- 브레인 카메라는 여러 개의 가상 카메라 중 하나를 현재 카메라로 사용함. 
- 시네머신 가상 카메라의 Follow에는 카메라가 움직이면서 추적할 대상을 할당함. 
- 시네머신 가상 카메라의 Look At에는 카메라가 회전하면서 조준할 대상을 할당함. 
- 시네머신 카메라의 데드존, 소프트존, 하드 리밋으로 카메라 추적의 강도를 조절할 수 있음

C# 인터페이스
- 인터페이스란 외부와 통신하는 공개 통로이며, 통로의 규격임.
- IItem 인터페이스를 상속한 클래스는 Use ( ) 메서드를 구현했음이 보장됨. 
- 어떤 오브젝트가 IItem 타입으로 취급 가능하다면 구체적인 타입을 검사하지 않고 Use( ) 메서드를 실행할 수 있음

유니티에서 연기, 화염재 등의 시각 효과는 파티클 시스템Particle System 컴포넌트를 사용함.
코루틴 - 동작 사이에 쉬는시간을 넣는? (이 게임에서의 경우 약간의 대기를 넣어서 총을 쐈을때의 탄알 궤적을 확인가능)

Raycast - 레이캐스트는 보이지 않는 광선을 쐈을 때 광선이 다른 콜라이더와 충돌하는지 검사하는 처리임.

한 스크립트에선 그 스크립트에 대한 것만 분리해서 처리해주는것이 유지보수, 관리면에서 더 편함

오브젝트에 ui를 붙히고자 하면 오브젝트에 자식으로 ui 오브젝트를 넣어서 설정하면 된다?

내비게이션 시스템
- 유니티는 한 위치에서 다른 위치로의 경로를 계산하고 실시간으로 장애물을 피하며 이동하는 인공지능을
만드는 내비게이션 시스템을 제공함 / 실시간으로 생성되지 않으므로 미리 구워야 함
NavMesh - 에이전트가 걸어다닐 수 있는 표면
NavMesh Agent - 내비메시 위에서 경로를 계산하고 이동하는 캐릭터 또는 컴포넌트
NavMesh Obstacle - 에이전트의 경로를 막는 장애물
Off Mesh Link - 끊어진 내비메시 영역 사이를 잇는 연결 지점(뛰어넘을 수 있는 울타리나 타고 올라갈 수 있는 담벼락을 구현하는 데 사용)

overlapsphere - 설정해놓은 반경 내에 있는 모든 콜라이더를 감지함
	
◦ Action은 입력과 출력이 없는 메서드를 등록할 수 있는 델리게이트 타입임.
◦ 이벤트는 연쇄적인 처리를 발생시키는 사건임.
◦ 이벤트를 사용해 한 클래스에서 일어난 사건을 다른 클래스에서 알 수 있음.
◦ 이벤트를 구독하는 메서드를 이벤트 리스너라고 부름.
◦ 이벤트는 클래스 외부로 델리게이트 변수를 공개하여 구현할 수 있음.

◦ 슬라이더 컴포넌트는 Value 값에 따라 슬라이더를 채움.
◦ 슬라이더 컴포넌트는 슬라이더의 모습을 그리지 않음.
◦ Fill 게임 오브젝트의 이미지 컴포넌트가 슬라이더의 모습을 그림.

Lerp( ) 메서드는 보간값을 기준으로 시작값과 도착값 사이의 중간값을 반환함

포스트 프로세싱은 게임 화면이 표시되기 전에 삽입하는 이미지 처리임. 
◦ 유니티는 포스트 프로세싱 스택 패키지로 포스트 프로세싱을 제공함. 
◦ 포스트 프로세스 레이어 컴포넌트는 포스트 프로세스를 적용함. 
◦ 포스트 프로세스 볼륨 컴포넌트는 포스트 프로세싱 레이어에 효과를 전달함. 
◦ 포스트 프로세스 프로파일은 미리 사용할 효과 목록을 기록하는 프리셋 파일임. 
◦ 포스트 프로세스 볼륨 컴포넌트에 포스트 프로세스 프로파일을 할당함.
